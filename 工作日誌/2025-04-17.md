---
updated: 2025-08-26 13:16
created: 2025-04-17 14:11
---
```widgets
type: countdown
date: 2025-07-19 13:45:00
to:
```


> [!SUCCESS] 完成
> - 藥理藥化 108-1、108-2、107-1

> [!FAQ] 追蹤
>  - 需要考察使用情況


```dataviewjs
let qNumber = "114-1";
let subNumber = "2";
let folderFilter = "!一階國考題/藥理藥化";
let pattern = new RegExp(`^\\s*#####\\s+${qNumber}\\(${subNumber}\\)[\\s\\S]*?(?=\\?\\?\\?|\\Z)`, "gm");

let pages = dv.pages().where(p =>
  p.file.path.endsWith(".md") && p.file.path.includes(folderFilter)
);
let found = false;

for (let page of pages) {
  let content = await dv.io.load(page.file.path);
  let matches = [...content.matchAll(pattern)];

  if (matches.length > 0) {
    found = true;
    dv.header(4, `📘 ${page.file.name} 中找到：`);
    matches.forEach((m, i) => {
      // 用原始檔案名建立內部連結 + 程式區塊顯示
      dv.paragraph(`➡️ [[${page.file.name}#${qNumber}(${subNumber})]]`);
      dv.paragraph("```\n" + m[0].trim() + "\n```");
    });
  }
}

if (!found) {
  dv.paragraph("❌ 沒有在整個 Vault 中找到該題目。");
}

```